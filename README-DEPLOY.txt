MYOS RECRUITMENT DEMO — READY FOR GITHUB PAGES
Author: Maria Szczudło

This folder is the READY-TO-PUBLISH static site. No npm build is required.
Upload the CONTENTS of this folder to a dedicated GitHub repository.

Recommended repository name:
myos-recruitment-demo

Expected URL:
https://marysiaszczudlo.github.io/myos-recruitment-demo/

GitHub Pages setup:
1. Create the repository `myos-recruitment-demo`.
2. Upload all files/folders from this package to the repository root.
3. Commit to `main`.
4. Repository → Settings → Pages.
5. Source: Deploy from a branch.
6. Branch: main, folder: / (root), then Save.
7. Wait for GitHub Pages to publish and open the HTTPS URL shown there.

ACCESS PASSWORD:
BlueMyOS-Maria26

RECRUITER EMAIL SHOULD CONTAIN ONLY:
- the HTTPS demo URL
- the password
- optionally the presentation and video walkthrough
Do NOT send this deployment ZIP to recruiters.

SECURITY NOTE:
This is a client-side password gate on a static GitHub Pages site. It is useful against casual access, and the password is stored as a SHA-256 hash rather than plain text. However, GitHub Pages is public static hosting, so this is not server-side authentication and can be bypassed by a technically skilled visitor inspecting the deployed source. The site also includes noindex/nofollow directives and robots.txt to discourage indexing.
